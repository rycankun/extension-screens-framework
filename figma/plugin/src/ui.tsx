/**
 * Figma Plugin UI Thread — TrustID Extension Screen Library
 *
 * React iframe rendered inside the Figma plugin panel.
 * Provides controls for token sync, screen import, and status display.
 * Communicates with the main thread (code.ts) via parent.postMessage.
 *
 * Placeholder — implementation will be built alongside screen components.
 *
 * @see figma/plugin/src/code.ts for the main thread handler
 */

export {};
