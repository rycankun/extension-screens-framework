/**
 * Plugin Manifest — Figma Plugin Shared Utilities
 *
 * Tracks which tokens and screens have been synced to the Figma file.
 * Enables diff-based updates so the plugin only touches what changed.
 *
 * Placeholder — implementation will be built in the plugin sync step.
 *
 * @see figma/plugin/manifest.json for the plugin metadata
 */

export {};
