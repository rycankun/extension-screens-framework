/**
 * Types Barrel Export — TrustID Extension Screen Library
 *
 * Single import point for all project TypeScript types:
 *   import type { Theme, ScreenVariantProps } from '@/types'
 */

export * from './screens';
export * from './tokens';
